# Sorting a Dictionary.marks = {	'Maths': 90,	'English': 78,	'Swahili': 67,	'Science': 54,	'Social Studies': 20}# Use the sorted() function to get the sorted version of the Dictionary.# By default, python sorts a Dictionary by the keys.print(sorted(marks))"""To sort the Dictionary by the values, treat the keys and values as 2 lists& use the zip() function to zip them together. The item you pass in first in the zip()function is what will be used to sort the Dictionary."""a = zip(marks.values(), marks.keys())# Sorted: Use the sorted() function.print(sorted(a))# Minimum: Use the min() function.# print(min(a))# Maximum: Use the max() function.# print(max(a))