"""Creating a file:Create a file object. Use the open() function with the name of the file to create &the 'w' to prepare the file for writing."""fc = open('soda.txt', 'w')# Use the write() function to write to the file.fc.write('This is a file.\n')fc.write('It\'s a file indeed!')# After writing, use the close() function to close the file.fc.close()"""Opening an existing file:Create a file object: Use the open() function with the name of file to open& the 'r' for reading."""fr = open('soda.txt', 'r')# Use the read() function to read the contents of the file.a = fr.read()print(a)# After reading, use the close() function to close the file.fr.close()